if (item.Type == 'GM'){
    if (action.InternalName=='Avaliacao_Concluida'){
        showMessage('Você tem certeza de que o anexo incluído é o FO-COR-GER-QSMS-011?');
        // abort("No abort o processo é finalizado e força o usuário a não seguir o fluxo");
    }
}